name: Demo CI Env Secrets

on:
    # push:
    workflow_dispatch:

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: say hello
              run: echo "Hello, World!"

            - name: install
              env:
                APP_ENV: demo
              run: echo "Env $APP_ENV"

